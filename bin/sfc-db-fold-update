#!/usr/bin/python3

import sys

from sfc_app import usage, fold_db_update

if __name__ == '__main__':
    if len(sys.argv) < 3:
        usage('project_file fold_csv_file')
        exit(0)
    fold_db_update(sys.argv[1], sys.argv[2])
