#!/usr/bin/python3

import sys

from sfc_app import usage, create_db_table

if __name__ == '__main__':
    if len(sys.argv) < 2:
        usage('project_file')
        exit(0)
    create_db_table(sys.argv[1])
