#!/usr/bin/python3

import sys

from sfc_app import usage, calculate_fold

if __name__ == '__main__':
    if len(sys.argv) < 3:
        usage('project_file output_fold_csv_file')
        exit(0)
    calculate_fold(sys.argv[1], sys.argv[2])
